version: "3.8"

services:
  # server_py:
  #   build: ./server_py
  #   container_name: streamdeck_server
  #   ports:
  #     - "5000:5000"
  #   volumes:
  #     - ./server_py:/app
  #   restart: always

  server_go:
    build: ./server_go
    container_name: streamdeck_server_go
    ports:
      - "5001:5001"
    environment:
      - IN_DOCKER=true